@model IEnumerable<SmartDrivingMVC.Models.PostalDistrict>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Zipcode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zipcode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PostalDistrictId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PostalDistrictId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PostalDistrictId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
